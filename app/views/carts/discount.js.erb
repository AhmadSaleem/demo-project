<% unless @total_discount.zero? %>
  $('#get-discount').hide();
<% end %>
$('#show-discount').text(<%= @total_discount %>);
$('#payable-amount').text("Payable amount: " + <%= @payable_amount %>);
$("#notice").html("<%= flash[:notice] %>");
$("#alert").html("<%= flash[:alert] %>");
