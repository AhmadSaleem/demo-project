<nav class="navbar navbar-default navbar-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
        <%= link_to "Demo Project", products_path, class: "navbar-brand" %>
    </div>
    <div id="navbar" class="collapse navbar-collapse">
      <ul class="nav navbar-nav navbar-right">
        <li class="navbar-form" role="search">
          <%= form_tag products_path, method: :get do %>
            <p>
              <%= text_field_tag :search, nil, placeholder: 'Search products', class: "form-control" %>
              <%= button_tag(type: :submit, class: "btn btn-default glyphicon glyphicon-search form-control") do %><% end %>
            </p>
          <% end %>
        </li>
        <% if user_signed_in? %>
          <li><%= link_to "New Product", new_product_path %></li>
          <li><%= link_to render_name(current_user), user_path(current_user) %></li>
          <li><%= link_to "Dashboard", dashboard_path %></li>
          <li><%= link_to "Sign out",  destroy_user_session_path, :method => :delete %></li>
        <% else %>
          <li><%= link_to "Sign in", user_session_path %></li>
        <% end %>
        <li>
          <%= link_to carts_show_path do %>
            <span class="glyphicon glyphicon-shopping-cart"></span>
            <span id="cart-items" class="badge"><%= @size %></span>
          <% end %>
        </li>
      </ul>
    </div><!--/.nav-collapse -->
  </div>
</nav>
